#!/usr/bin/env bash
# .agents/skills/ is the single source of truth.
# This script is kept for backward compatibility but is now a no-op.
# All platforms read directly from .agents/skills/.
echo "Skills are managed in .agents/skills/ — no sync needed."
