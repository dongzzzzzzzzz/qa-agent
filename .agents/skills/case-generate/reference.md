# 测试用例格式参考

默认 Markdown 表格列：

```text
用例编号 | 用例标题 | 需求模块 | 优先级 | 是否可自动化 | 前置条件 | 测试数据 | 执行步骤 | 预期结果 | 后置动作 | 需求依据
```

好用例标准：

- 标题说明验证目标，不写成操作流水账。
- 前置条件让别人知道从什么状态开始。
- 测试数据明确到可构造、可筛选、可 Mock。
- 执行步骤不超过 10 步。
- 预期结果必须能直接判断 pass/fail。
- 后置动作说明如何恢复环境。
